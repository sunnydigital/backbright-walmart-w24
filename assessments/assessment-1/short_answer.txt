1) What does the `rm` command in the terminal do?

Your answer:

Removes a file

------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:

`ls`

------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:

cd Users moves from the relative path of the current to the subfolder Users, while 
cd /Users moves from the root directory to a folder called Users

------------------
4) Describe what git is and what it is used for.

Your answer:

Git is a version control system to manage all the different versions of software

------------------
5) Describe what GitHub is and what it is used for.

Your answer:

Github is a service that operates the version control software Git, hosted online and owned by Microsoft

------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:

c) git init

------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:

c) git status

------------------
8) What is the difference between `git add` and `git commit`?

Your answer:

Git att stages your changes to comit, while git comit saves the changes to the local project's history

------------------
9) What does the `git push` command do?

Your answer:

Git push implements the changes to the remote folder hosting the shared version in question

------------------
10) When would you use the `git clone` command?

Your answer: To obtain a local version of a web-hosted git


